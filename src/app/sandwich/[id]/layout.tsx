import { SandwichDetailsPageLayout } from '@/features/sandwichDetails'
import { getMetadata } from '@/utils/seo'

export const metadata = getMetadata({
  title: 'Sandwich Details'
})

export default SandwichDetailsPageLayout
